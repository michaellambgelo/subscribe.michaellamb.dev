# Worker endpoint for the chatbot. In local dev, Vite proxies /api/* to
# wrangler dev, so leaving this unset is fine. For the GitHub Pages build,
# copy to `.env.production` and set it to your deployed worker URL.
VITE_CHAT_ENDPOINT=https://subscribe-chatbot.<your-account>.workers.dev/chat
